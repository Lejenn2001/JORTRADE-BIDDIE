What I confirmed already: Paper Trades page lives at /dashboard/paper-trades and is behind <ProtectedRoute>. Auth is Supabase email/password (with Google/Apple OAuth). Review Trade flow: signal card → PaperTradeTicket modal → calls POST /api/whale/paper/alternatives for the recommended contract + budget/safer alternatives → POST /api/whale/paper/trades to submit. Entry fill rule (conservative): Ask (fallback Mid → Last). Exit fill: Bid (fallback Mid → Last). Stored in entry_fill_source / exit_fill_source. P/L math: Open: (current_quote - entry_price) × contracts × 100 Closed: (exit_price - entry_price) × contracts × 100 Auto-close: paperTradeMonitor.ts polls every 60s during market hours (5 min off-hours), evaluates against signal_target / signal_invalidation using underlying price from priceMonitor.ts. Expiry auto-closes at 4:00 PM ET on expiry date. Real signals are present right now — e.g., an AMZN bearish 265P expiring May 1, 2026 is live in the feed. Two blockers before I can actually run the UI flow: Login credentials. The Paper Trades page redirects to /login without a session, and Supabase Auth doesn't expose any seeded test account. I need a test email + password I can use (ideally a non-admin paper-trading account you've set up for QA), or your go-ahead to use your own login. I won't store the credentials anywhere — they'll be passed straight to the test runner for that session. Market timing. Right now it's 4:29 PM ET on Friday, April 24, 2026 — the regular session just closed. That has two consequences: The "live" signal feed may show no fresh setups; I'll have to use a recent signal from history (the AMZN one above is a candidate). Auto-close on target/stop won't fire naturally during the test, because the underlying won't be moving on the live feed. We can still verify the auto-close mechanism is wired up (monitor running, target/stop persisted on the trade row), but a true target-hit / stop-hit observation would need a test during market hours on Monday — or a controlled price injection, which I will not do without your explicit approval. How would you like to proceed? (A) Send me a test login now, I run the full flow end-to-end against the AMZN signal (or whichever real signal you point me at), validate entry capture + P/L + manual close + that target/stop are correctly persisted, and we leave the live auto-close verification for a market-hours run on Monday. Recommended. (B) I run a backend-only validation now (no UI): hit the paper-trade endpoints directly with a service token to confirm contract pricing, P/L math, and close behavior, then defer the full UI test until you're available to log in. (C) Wait until Monday during market hours and run everything in one shot.